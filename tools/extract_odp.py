# Optional extraction helper
# This website already includes generated data in data/league-structures.js.
# The original generation script used the OpenDocument XML inside the ODP file.
# Keep this file as a reminder of the workflow: unzip ODP -> parse content.xml -> read draw:page frames/connectors.

print("The generated data are already in data/league-structures.js. Regenerate from the ODP with the build script if needed.")
